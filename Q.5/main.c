#include <stdio.h>
#include <stdlib.h>
/*
(5) Write a program to print the ASCII value of a character input by the user.
*/
int main()
{
    printf("enter character!\n");
    char x;
    scanf("%c",&x);
    printf("the ASCII value of a character is %d",x);

    return 0;
}
